<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="/static/js/reply_view.js"></script>
    <script>
        window.onload = () => {
            const num = document.getElementById("number")
            console.log("num : ", num)
            console.log("num.innerHTNL : ", num.innerHTML)
            init(num.innerHTML)
        }
    </script>
</head>

<body align="center">
    <%- include ("./reply_form") %>
        <h1>- 개 인 정 보 -</h1>
        <table align="center" border="1">
            <tr>
                <th>글 번호</th><td id="number"><%= data.WRITE_NO %></td>
                <th>작성자</th>
                <td>
                    <%= data.ID %>
                </td>
            </tr>
            <tr>
                <th>제목</th>
                <td>
                    <%= data.TITLE %>
                </td>
                <th>등록일자</th>
                <td>
                    <%= data.SAVE_DATE %>
                </td>
            </tr>
            <tr>
                <th>내용</th>
                <td>
                    <%= data.CONTENT %>
                </td>
                <th colspan="2">
                    <% if(data.ORIGIN_FILE_NAME=="nan" ){ %>
                        이미지가 없습니다.
                        <% } %>
                            <a href="/board/download/<%= data.CHANGE_FILE_NAME %>"><img
                                    src="/board/download/<%= data.CHANGE_FILE_NAME %>"></a>
                </th>
            </tr>
        </table>
        <% if(data.ID==username){ %>
            <button onclick="location.href='/board/modify_form/<%= data.CHANGE_FILE_NAME %>'">수정하기</button>
            <button onclick="location.href='/board/del/<%= data.CHANGE_FILE_NAME %>'">삭제하기</button>
            <% } %>
                <button onclick="reply_form()">답글달기</button>
                <button onclick="location.href='/board/list'">리스트로 돌아가기</button>
                <table>
                    <tr>
                        <td colspan="4" id="content"></td>
                    </tr>
                </table>
</body>

</html>