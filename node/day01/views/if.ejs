<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <% if(num> 90) { %>
        num 90보다 크다 <%= num %>
            <% } else if(num> 80) { %>
                num 80보다 크다 <%= num %>
                    <% } else { %>
                        else = > <%= num %>
                            <% } %>
</body>

</html>