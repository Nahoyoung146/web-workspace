<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <%- include("../test") %>
    <% result.forEach( item=> { %>
        id : <%= item.ID %><br>
            pwd : <%= item.PWD %><br>
                name : <a href="/member/member_view/<%= item.ID %>">
                    <%= item.NAME %>
                </a>
                <br>
                addr : <%= item.ADDR %>
                    <hr>
                    <% }) %>
                        <a href="/member/register_form">데이터 추가</a>
</body>

</html>