<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body align="center">
    <% if ( username===undefined ) { %>
        <a href="/">HOME</a>
        <a href="/member/login_form">로그인</a><br>
        <hr>

        <% } else if ( !username ) { %>
            <a href="/">HOME</a>
            <a href="/member/login_form">로그인</a><br>
            <hr>

            <% } else { %>
                <a href="/">HOME</a>
                <a href="/member/logout">로그아웃</a><br>
                <hr>
</body>

</html>